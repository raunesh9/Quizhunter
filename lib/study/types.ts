export type Slide = { number: number; title: string; text: string; notes: string; images: string[]; source?:string; sourceSlide?:number };
export type Deck = { name: string; slides: Slide[]; warnings: string[]; sample?: boolean; documents?:string[] };
export type Lesson = { slide: number; title: string; bigIdea: string; explanation: string[]; terms: {term:string;definition:string}[]; example: string; connections: string; misconceptions: string[]; check: {question:string;answer:string}; limitations:string[] };
export type Card = {front:string;back:string;slide:number};
export type Quiz = {question:string;options:string[];answer:number;explanation:string;slide:number};
export type StudyMode = 'explain' | 'cards' | 'quiz' | 'ask' | 'agent';
export type StudySettings = {depth:'detailed'|'simple'|'exam';level:'beginner'|'college'|'advanced';pace?:'quick'|'thorough'};
export type HiddenContent = {cards:string[];quiz:string[];lessons:number[]};
export type StudyProgress = {cards:number[]; quiz:number[]};
export type AgentSlideResult = {lesson:Lesson; cards:Card[]; quiz:Quiz[]; practiceStatus:'ready'|'not_applicable'; practiceNote:string};
export type AgentResults = Record<string,AgentSlideResult>;
export type StudyPack = {version:1; deck:Deck; lessons:Record<string,Lesson>; cards:Card[]; quiz:Quiz[]; progress?:StudyProgress; agentResults?:AgentResults;hidden?:HiddenContent;settings?:StudySettings};
